server:
  port: ${SERVER_PORT:8083}

spring:
  application:
    name: catalog-service

  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/catalog_db?replicaSet=rs0}

# Mongock cumpre para o MongoDB o papel do Flyway no SQL — ADR-017.
mongock:
  migration-scan-package:
    - com.deliveryplatform.catalog.infrastructure.persistence.migration

  rabbitmq:
    addresses: ${RABBITMQ_ADDRESSES:localhost:5672}
    username: ${RABBITMQ_USERNAME}
    password: ${RABBITMQ_PASSWORD}
    publisher-confirm-type: correlated
    publisher-returns: true
    listener:
      simple:
        acknowledge-mode: manual
        retry:
          enabled: true
          initial-interval: 2s
          max-attempts: 5

  data:
    redis:
      url: ${REDIS_URL:redis://localhost:6379}

  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: ${JWT_JWKS_URI:http://localhost:8081/.well-known/jwks.json}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never

springdoc:
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    root: INFO
    com.deliveryplatform: INFO
  pattern:
    # correlationId entra desde o início: retrofit em payload de evento é caro.
    console: "%d{yyyy-MM-dd HH:mm:ss} %-5level [%X{traceId:-},%X{spanId:-}] %logger{36} - %msg%n"
