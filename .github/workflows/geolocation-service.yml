name: geolocation-service

on:
  push:
    paths:
      - 'backend/services/geolocation-service/**'
      - 'backend/build-logic/**'
      - 'backend/gradle/libs.versions.toml'
      - '.github/workflows/geolocation-service.yml'
  pull_request:
    paths:
      - 'backend/services/geolocation-service/**'
      - 'backend/build-logic/**'
      - 'backend/gradle/libs.versions.toml'

jobs:
  ci:
    uses: ./.github/workflows/_service-ci.yml
    with:
      module: ':services:geolocation-service'
