name: merchant-service

on:
  push:
    paths:
      - 'backend/services/merchant-service/**'
      - 'backend/build-logic/**'
      - 'backend/gradle/libs.versions.toml'
      - '.github/workflows/merchant-service.yml'
  pull_request:
    paths:
      - 'backend/services/merchant-service/**'
      - 'backend/build-logic/**'
      - 'backend/gradle/libs.versions.toml'

jobs:
  ci:
    uses: ./.github/workflows/_service-ci.yml
    with:
      module: ':services:merchant-service'
